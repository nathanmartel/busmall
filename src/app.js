import { initializeStorage, 
    initializeAdminButtons, 
    initializeListenToProducts,
    renderNewImages } from './vote.js';

// Run On Load
initializeStorage();
initializeAdminButtons();
initializeListenToProducts();
renderNewImages();
